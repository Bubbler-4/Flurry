(executables
  (names module1)
  (libraries js_of_ocaml-lwt)
  (modes byte)
  (preprocess (pps js_of_ocaml-ppx)))

(rule
  (targets module1.js)
  (action
    (run %{bin:js_of_ocaml} --opt=3 -o %{targets} %{dep:module1.bc})))

(alias
  (name default)
  (deps module1.js))